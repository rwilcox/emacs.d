(provide 'theme-mine)

 ; current status of theme:
; using mousemacs theme, so set in mousemacs/mousemacs-theme to load correct built in theme

; but for when I'm not, set it here...
                                        ; (use-package color-theme-sanityinc-solarized)

;    (use-package cloud-theme
;        :ensure t
;        :config
;        (load-theme 'cloud t))

; (use-package base16-theme)
